<UserControl
    x:Class="Boards_WP.Views.CommentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Boards_WP.Views">

    <Grid Margin="{x:Bind GetIndentMargin(CommentData.Indentation), Mode=OneWay}" Padding="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Width="2" Background="#6A67E1" Margin="0,5,15,5"
                Visibility="{x:Bind GetLineVisibility(CommentData.Indentation), Mode=OneWay}" CornerRadius="1"/>

        <Border Grid.Column="1" Background="White" CornerRadius="12" Padding="15" BorderBrush="#E0E0E0" BorderThickness="1">
            <StackPanel>
                <Grid ColumnDefinitions="Auto, *">
                    <StackPanel Grid.Column="0" Margin="0,0,15,0" Spacing="2">
                        <Button Command="{x:Bind ViewModel.UpvoteCommand}" Content="▲" Foreground="{x:Bind ViewModel.UpvoteColor, Mode=OneWay}" Background="Transparent" BorderThickness="0"/>
                        <TextBlock Text="{x:Bind ViewModel.CommentData.Score, Mode=OneWay}" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <Button Command="{x:Bind ViewModel.DownvoteCommand}" Content="▼" Foreground="{x:Bind ViewModel.DownvoteColor, Mode=OneWay}" Background="Transparent" BorderThickness="0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Spacing="5">
                        <TextBlock Text="{x:Bind CommentData.Owner.Username}" FontWeight="Bold" Foreground="#6A67E1"/>
                        <TextBlock Text="{x:Bind CommentData.Description}" TextWrapping="Wrap"/>
                        
                        <Button Command="{x:Bind ViewModel.ToggleReplyCommand}" Background="Transparent" HorizontalAlignment="Right">
                            <StackPanel Orientation="Horizontal" Spacing="5">
                                <FontIcon Glyph="&#xE8BD;" FontSize="14" Foreground="#6A67E1"/>
                                <TextBlock Text="reply" FontSize="12" Foreground="#6A67E1"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>

                <StackPanel Visibility="{x:Bind ViewModel.IsReplyAreaVisible, Mode=OneWay}" Margin="40,10,0,0" Spacing="8">
                    <TextBox Text="{x:Bind ViewModel.ReplyText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                             PlaceholderText="Write a reply..." AcceptsReturn="True"/>
                    <Button Content="Post Reply" Command="{x:Bind ViewModel.SubmitReplyCommand}" 
                            HorizontalAlignment="Right" Background="#6A67E1" Foreground="White" CornerRadius="8"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>