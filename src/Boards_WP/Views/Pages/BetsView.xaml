<Page
    x:Class="Boards_WP.Views.Pages.BetsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Boards_WP.Views"
    xmlns:models="using:Boards_WP.Data.Models"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Padding="20" Background="#1A1A2E">
            <TextBlock Text="Weaponized Betting" FontSize="24" FontWeight="Bold" Foreground="White"/>
        </StackPanel>

        <ListView Grid.Row="1" 
              ItemsSource="{Binding TestBets}" 
              Padding="20" 
              SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Bet">
                        <local:BetItemControl BetData="{Binding}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>