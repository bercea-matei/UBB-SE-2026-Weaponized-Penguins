<UserControl
    x:Class="Boards_WP.Views.CommentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:Boards_WP.Data.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="0,0,0,10" 
          PointerEntered="Grid_PointerEntered" 
          PointerExited="Grid_PointerExited">
        
        <StackPanel>
            <Border BorderBrush="#D1D1D1" BorderThickness="1" CornerRadius="12" Padding="12" Background="White">
                <Grid ColumnDefinitions="40, *, Auto">
                    
                    <StackPanel Grid.Column="0">
                        <Button Content="▲" Command="{x:Bind ViewModel.UpvoteCommand}" Style="{StaticResource TextBlockButtonStyle}"/>
                        <TextBlock Text="{x:Bind ViewModel.CommentData.Score, Mode=OneWay}" HorizontalAlignment="Center" FontWeight="Bold"/>
                        <Button Content="▼" Command="{x:Bind ViewModel.DownvoteCommand}" Style="{StaticResource TextBlockButtonStyle}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="{x:Bind ViewModel.CommentData.Owner.Username, Mode=OneWay}" FontSize="12" FontWeight="Bold" Foreground="#6A67E1"/>
                        <TextBlock Text="{x:Bind ViewModel.CommentData.Description, Mode=OneWay}" TextWrapping="Wrap" Margin="0,5,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Spacing="10">
                        <StackPanel HorizontalAlignment="Center">
                            <Button Background="Transparent" BorderThickness="0" Command="{x:Bind ViewModel.ToggleReplyCommand}">
                                <FontIcon Glyph="&#xE90A;" FontSize="18" Foreground="#6A67E1"/>
                            </Button>
                            <TextBlock Text="reply" HorizontalAlignment="Center" FontSize="10" Foreground="#6A67E1"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>

           <StackPanel Visibility="{x:Bind ViewModel.IsReplyAreaVisible, Mode=OneWay}" 
                Margin="20,10,0,0" Spacing="10">
                <TextBox Text="{x:Bind ViewModel.ReplyText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                         PlaceholderText="Write a reply..." AcceptsReturn="True" CornerRadius="8"/>
                <Button Content="Post Reply" Click="PostReply_InternalClick" Background="#6A67E1" Foreground="White" HorizontalAlignment="Right" CornerRadius="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>